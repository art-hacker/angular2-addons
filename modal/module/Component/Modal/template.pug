.modal
  .modal-center
    .modal-box([ngStyle]="{width: width, height: height}", [ngClass]="{animation: animation}")
      a.close((click)="close()", *ngIf="canClose")
        i.fa.fa-times
      ng-content

  .backdrop(*ngIf="backdrop", (click)="canClose && backdropClose && close()")
